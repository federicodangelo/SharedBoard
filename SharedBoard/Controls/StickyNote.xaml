<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SharedBoard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:SharedBoard.Controls"
    x:Class="SharedBoard.Controls.StickyNote"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Tapped="StickyNote_Tapped"
    DoubleTapped="StickyNote_DoubleTapped">

    <Border BorderBrush="Black" BorderThickness="2" ManipulationMode="TranslateX,TranslateY" ManipulationDelta="StickyNote_ManipulationDelta" ManipulationStarted="StickyNote_ManipulationStarted">
        <Canvas Width="400" Height="300" Background="Yellow">
            <TextBox x:Name="textBox" TextWrapping="Wrap" AcceptsReturn="True" HorizontalAlignment="Stretch" Margin="0,0,0,0" Text="Sticky Note" VerticalAlignment="Stretch" FontSize="22" Width="400" Height="300" BorderBrush="{x:Null}" InputScope="Text" Background="{x:Null}" LostFocus="TextBox_LostFocus"/>
            <TextBlock x:Name="textBlock" TextWrapping="Wrap" HorizontalAlignment="Stretch" Margin="12,3,0,0" Text="Sticky Note" VerticalAlignment="Stretch" FontSize="22" Height="300" Width="397"/>
        </Canvas>
    </Border>
</UserControl>
